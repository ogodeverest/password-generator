import type Settings from "../models/Settings.interface";
import type { SettingsItem } from "../models/Settings.interface";

export default function generatePassword({ length, ...rest }: Settings) {
  let password: string = "";
}
